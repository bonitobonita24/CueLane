// Wave 7.6-T6 — Windows admin tab. Same thin server→client handoff as services/page.tsx.
import { WindowsClient } from './windows-client';

export default function WindowsPage() {
  return <WindowsClient />;
}
