// Wave 7.6-T7 — Users admin tab. Same thin server→client handoff as services/windows pages.
import { UsersClient } from './users-client';

export default function UsersPage() {
  return <UsersClient />;
}
